# 🚀 COMANDOS URGENTES PARA EJECUTAR EN PRODUCCION
# ================================================

# 1. CONECTAR AL SERVIDOR
ssh ubuntu@206.62.139.11

# 2. NAVEGAR AL DIRECTORIO
cd /home/l4bsb/Parroquia

# 3. VERIFICAR QUE TIENES LA CORRECCIÓN
grep -n "export default new FamiliasConsultasService" src/services/familiasConsultasService.js

# 4. SI NO APARECE LA CORRECCIÓN, APLICARLA:
sed -i 's/export default FamiliasConsultasService;/export default new FamiliasConsultasService();/g' src/services/familiasConsultasService.js

# 5. VERIFICAR QUE SE APLICÓ:
tail -3 src/services/familiasConsultasService.js

# 6. REINICIAR PM2 (CRÍTICO):
pm2 restart all

# 7. VERIFICAR ESTADO:
pm2 status

# 8. VERIFICAR LOGS:
pm2 logs --lines 10

# 9. PROBAR API:
curl http://localhost:3000/api/health

# 10. VERIFICAR QUE NO HAY MÁS ERRORES:
tail -20 logs/pm2-error.log | grep "is not a function"

# Si sale vacío = ERROR SOLUCIONADO ✅

# Production Environment Variables
# Copy this file to .env and update with your actual values

NODE_ENV=production
PORT=3000

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=parroquia_db
DB_USER=parroquia_user
DB_PASS=ParroquiaSecure2025

# JWT Configuration - CHANGE THESE SECRETS!
JWT_SECRET=your_super_secure_jwt_secret_change_this_in_production
JWT_REFRESH_SECRET=your_super_secure_refresh_secret_change_this_too
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Security
BCRYPT_ROUNDS=12

# Frontend Configuration
FRONTEND_URL=http://206.62.139.100

# Email Configuration (Optional - set SEND_REAL_EMAILS=true to enable)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
SMTP_FROM_EMAIL=
SEND_REAL_EMAILS=false

# Logging
VERBOSE_LOGGING=true
